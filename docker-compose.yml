services:
  app:
    build: .
    ports:
      - "8765:8765"
    env_file:
      - .env
    volumes:
      - ./config.yaml:/app/config.yaml
      - ./.env:/app/.env
      - ./data:/app/data
    restart: unless-stopped
